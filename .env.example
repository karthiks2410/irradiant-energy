# Copy to .env.local and fill in. .env.local is gitignored. Never commit real values.

# --- Public (safe in the client bundle) ---
# Canonical origin. Leave unset on previews; set to https://irradiantenergy.in once the domain is live.
# Setting it in production also switches search-engine indexing on (src/lib/env.ts).
NEXT_PUBLIC_SITE_URL=

# --- Server-only. Never prefix with NEXT_PUBLIC_. ---
# Resend (transactional email for quote requests). Set the values in Vercel → Project → Settings → Environment Variables.
RESEND_API_KEY=
# Sender and lead-alert recipient (decisions.md D-009). Move to the new domain once it is verified in Resend.
EMAIL_FROM=do-not-reply@irradiantenergie.com
LEAD_EMAIL=leads@irradiantenergie.com
