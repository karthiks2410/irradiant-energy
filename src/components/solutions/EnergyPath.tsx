/**
 * The Energy Path brand device has one implementation, in `@/components/pages/EnergyPath`, so the
 * same device cannot drift between the About page and the audience pages. This module stays only
 * so the audience-page route keeps its existing import path.
 */
export { EnergyPath } from "@/components/pages/EnergyPath";
